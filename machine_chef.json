{
    "builders":[{
        "type": "docker",
        "image": "ubuntu",
        "export_path": "./image.tar"
    }],
    "provisioners":[{
        "type": "chef-solo",
        "cookbook_paths": ["./site-cookbooks"],
        "run_list": ["apache::default"]
    }],
    "post-processors": [
        {
            "type": "docker-import",
            "repository": "tcnksm/packer",
            "tag": "0.1"
        },
        "docker-push"
    ]
}
